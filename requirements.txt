# Pipeline de fichas técnicas FAMIQ — dependencias
# Instalar en el runner self-hosted una sola vez (ver SETUP.md):
#   pip install -r requirements.txt
#   playwright install chromium

jinja2>=3.1
pydantic>=2.6
playwright>=1.40

# Gate 1 (extracción de normas): scripts/extraer_norma.py llama a la API
# de Anthropic con el PDF de la norma (necesita ANTHROPIC_API_KEY).
anthropic>=0.69
